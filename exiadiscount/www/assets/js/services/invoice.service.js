export class InvoiceService {
    
    create(datas) {
        console.log("Upload invoice: ", datas);
        alert("Une facture a bien été crée, malheureusement le service de gestion des factures n'est pas encore disponible :/")
    }
}